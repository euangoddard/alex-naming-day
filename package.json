{
  "name": "alex-naming-day",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build -d dist index.html",
    "deploy": "rm -rf dist && npm run build && echo 'alex-naming.euans.space' > dist/CNAME && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^1.2.0",
    "node-sass": "^4.9.2",
    "parcel-bundler": "^1.9.7"
  }
}
